``tsc --init `` to make config file
in config file change ``"outDir": "./",`` to where you wanr=t to compile and make the index.js file

```tsc -w``` for starting an continous server and will compile the code continously 

